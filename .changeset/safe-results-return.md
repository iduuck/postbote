---
"@postbote/plugin-better-result": minor
---

Initial release of `@postbote/plugin-better-result`, which changes `send()` to return `Result<SendResult, PostboteError>` and captures transformation, validation, middleware, and adapter failures.
