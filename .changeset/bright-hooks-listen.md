---
"@postbote/plugin-hooks": minor
---

Initial release of `@postbote/plugin-hooks` with validated `transformMessage`, `beforeSend`, `afterSend`, and `onError` lifecycle hooks plus non-retryable policy cancellation.
