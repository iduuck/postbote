---
"@postbote/plugin-react-email": minor
---

Initial release of `@postbote/plugin-react-email` with typed `ReactElement` message bodies rendered to HTML and optional plain text before normalization.
